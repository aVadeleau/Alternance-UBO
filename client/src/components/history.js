import {createBrowserHistory} from "history";
/*
 * This file is used to redirect on different pages
 * For example when we register a student it's used to redirect to the login page  
 */
export default createBrowserHistory();
